import { GetClassActualStatePipe } from './get-class-actual-state.pipe';

describe('GetClassActualStatePipe', () => {
  it('create an instance', () => {
    const pipe = new GetClassActualStatePipe();
    expect(pipe).toBeTruthy();
  });
});
