import { ProcessStateModel } from './process-state-model';

describe('ProcessStateModel', () => {
  it('should create an instance', () => {
    expect(new ProcessStateModel()).toBeTruthy();
  });
});
